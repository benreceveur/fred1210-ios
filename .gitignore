# Xcode
*.xcodeproj/xcuserdata
*.xcworkspace/xcuserdata
DerivedData/
build/
.build/
*.ipa
*.dSYM.zip
*.dSYM

# xcodegen generated — regenerate via `xcodegen generate`
Fred1210.xcodeproj
Fred1210/Fred1210.entitlements
Fred1210/Info.plist
ShareExtension/Info.plist
ShareExtension/Fred1210ShareExtension.entitlements

# macOS
.DS_Store

# SPM
Package.resolved
.swiftpm/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# ─── Personal setup & secrets (must never be committed) ─────────────────────
# Credentials of any kind
*.p8
*.p12
*.pem
*.key
*.cer
*.crt
*.keystore
*.jks
*.mobileprovision
*.provisionprofile
AuthKey_*.txt
AuthKey_*.p8
service-account*.json
*-credentials.json
*-creds.json
google-services.json
GoogleService-Info.plist

# Per-developer Xcode config (holds Apple Team ID, Fred host URL, etc.)
# See Config/Local.xcconfig.example for the template.
Config/Local.xcconfig

# Per-user / per-machine local config
.env
.env.*
!.env.example
.env.local
*.local.json
.personal/
.personal.*
secrets/
local-config/

# Fastlane session / auth cache
fastlane/Appfile.local
fastlane/.env
fastlane/.env.*
!fastlane/.env.example
